// The quiz's numbers in one place — mechanics.md's table, spelled as code.
// Tuning the game is an edit here, never a hunt through components.
export const START_SCORE = 1000
export const CLUE_COST = 150
export const WRONG_GUESS_COST = 100
export const WIN_FLOOR = 100
export const STREAK_BONUS = 50
export const SESSION_ROUNDS = 5
